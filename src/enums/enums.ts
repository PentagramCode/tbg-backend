/**
 * This enums describes the different roles
 * @typeParam admin: string - role of administrator
 * @typeParam user: string - role of user
 */
export enum Roles {
    admin = 'ADMIN_ROLE',
    user = 'USER_ROLE'
}